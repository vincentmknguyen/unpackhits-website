{
  "name": "unpackhits-website",
  "version": "1.0.0",
  "private": true,
  "description": "Unpack Hits — Pokemon bulk card buyer. Static site with serverless form pipeline.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@notionhq/client": "^2.3.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
